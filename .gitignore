node_modules
.next
.env.local
.env*.local
.contentlayer
*.log
.DS_Store

# Source media stays out of git (the 97 MB 4K original lives in ../Media);
# the web-optimised derivatives under public/media DO ship — Vercel serves them.
*.mp4
*.mov
pexels-*.jpg
!public/media/*.mp4
!public/media/*.webm
